{
  "name": "@vault-db/core",
  "license": "Apache-2.0",
  "dependencies": {
    "buffer": "^6.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "esbuild": "^0.23.1",
    "mocha": "^9.2.2"
  },
  "scripts": {
    "test": "mocha --recursive --exclude 'test/web/*'",
    "test:bundle": "esbuild --bundle test/web/index.js --outfile=test/web/bundle.js --watch --servedir=."
  }
}
